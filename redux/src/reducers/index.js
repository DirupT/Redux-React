const initialState = {
    fetchingUsers: false,
    usersFetched: false,
    error: null
}

export default (state, action) => {
    switch (action.type) {
        default:
            return state;
    }
}